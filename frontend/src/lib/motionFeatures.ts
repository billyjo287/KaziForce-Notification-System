// Motion's animation engine (including layout animations for list reordering), loaded as a
// separate download right after the first paint so it does not delay the app shell.
import { domMax } from 'motion/react';

export default domMax;
