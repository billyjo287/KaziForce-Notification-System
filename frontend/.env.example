# Frontend settings. Copy to .env (`npm run setup` does this for you).
# Only variables starting with VITE_ reach the browser, so never put secrets here.

# Address of the backend API.
VITE_API_URL=http://localhost:4000
